const myTodos = []

myTodos.push('Buy bread')
myTodos.push('Record videos for youtube')
myTodos.push('Go to gym')

myTodos.forEach((value, index) => { console.log(`your task no ${index + 1} is ${value}`); })

